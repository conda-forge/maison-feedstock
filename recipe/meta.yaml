{% set version = "2.0.1" %}

package:
  name: maison
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/m/maison/maison-{{ version }}.tar.gz
  sha256: 15e6e77cab74db796526a4eefedf1c072de16323834c01b182e9b674cfdd8fcc

build:
  entry_points:
    - maison = maison.__main__:main
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - poetry-core
    - pip
  run:
    - python >={{ python_min }}
    - click
    - toml

test:
  imports:
    - maison
  commands:
    - pip check
    - maison --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/dbatten5/maison
  summary: Read settings from config files
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - danielnachun
